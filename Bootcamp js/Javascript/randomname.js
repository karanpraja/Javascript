function whosPaying(names) {
    
    /******Don't change the code above*******/
        
        //Write your code here.
        
       const name=names;//const because this is a special case
        let l=name.length;
        var rname=;//do not add one because it will print error limit=0-5
        rname=Math.floor(rname);
        var output=name[rname]+" is going to buy lunch today!";
        return output;
        
        
        
    
    
    /******Don't change the code below*******/    
    }